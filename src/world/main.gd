extends Node2D

var world_map=[{ "name": &"room1", "global_position": (1024.0, 0.0), "size": (1280.0, 720.0) }, { "name": &"Surface1", "global_position": (0.0, 0.0), "size": (1024.0, 1024.0) }]

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
